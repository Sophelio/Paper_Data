# Exact elimination derivation

Starting from the standardized regression and the affine Y-phaseder map `z=((y+s)/(d+s)-mu)/sigma`, collect coefficients of `y` to obtain `A` and move remaining terms into `B`. Discharge-specific coefficients and per-discharge `(s, mu, sigma)` are required; there is no single cohort-mean eliminated equation.
